# Copy to .env and fill values
SUPABASE_URL=
SUPABASE_KEY=
# Optional: URL of Stock Service to notify after sales (POST /stock/move)
STOCK_SERVICE_URL='http://localhost:3002'
# Port for this service
PORT=4000
